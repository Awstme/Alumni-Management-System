#include "alumni_list.h"
#include <iostream>
#include <fstream>

using namespace std;

void AlumniList::load()
{
    ifstream inFile("../alumni_data.txt");
    // 空文件检测 待写
    while (!inFile.eof())
    {
        
    }
}